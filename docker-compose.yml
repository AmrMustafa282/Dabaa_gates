services:
  app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - gates-data:/app/data
    environment:
      - DATABASE_URL=file:/app/data/gates.db
      - HEALTH_CHECK_INTERVAL_MS=10000
    cap_add:
      - NET_RAW
    restart: unless-stopped

volumes:
  gates-data:
